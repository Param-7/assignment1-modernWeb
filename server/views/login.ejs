<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-slate-200 h-screen flex flex-col"> 
    <!-- Header -->
    <header class="bg-white shadow-md fixed top-0 left-0 right-0 z-50">
        <nav class="flex justify-between items-center px-6 py-4">
            <a href="/" class="text-blue-800 text-xl font-semibold">Home</a>
        </nav>
    </header>

    <!-- Main Content -->
    <main class="flex-grow flex items-center justify-center pt-20 px-4">
        <div class="bg-white rounded-lg shadow-lg p-8 w-full max-w-md">
            <h1 class="text-2xl font-bold text-gray-900 mb-4">Login</h1>
            <% if (error) { %>
                <p class="text-red-500 mb-4"><%= error %></p>
            <% } %>
            <form method="POST" action="/apiRoutes/login" class="space-y-4">
                <div>
                    <label for="username" class="block text-sm font-medium text-gray-700">Username</label>
                    <input
                        type="text"
                        id="username"
                        name="username"
                        class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500"
                        placeholder="John_01"
                        required
                    />
                </div>
                <div>
                    <label for="password" class="block text-sm font-medium text-gray-700">Password</label>
                    <input
                        type="password"
                        id="password"
                        name="password"
                        class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500"
                        placeholder="********"
                        required
                    />
                </div>
                <button
                    type="submit"
                    class="w-full bg-indigo-600 text-white py-2 rounded-md text-sm font-semibold hover:bg-indigo-500 focus:ring focus:ring-indigo-300">
                    Login
                </button>
                <p class="text-sm text-center mt-4">
                    Don't have an account? 
                    <a href="/register" class="text-indigo-600 hover:underline">Register here</a>
                </p>
            </form>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-gray-400 text-center py-4">
        <p>&copy; <%= new Date().getFullYear() %> Login Authentication. All rights reserved.</p>
    </footer>
</body>

</html>
